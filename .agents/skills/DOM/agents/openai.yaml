interface:
  display_name: "DOM"
  short_description: "Repo-specific patterns and workflows for DOM"
  default_prompt: "Use the DOM repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true